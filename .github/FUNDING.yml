# Placeholder — no sponsor account exists yet. Uncomment the relevant line(s)
# once one does, and update FUNDING_URL in src/app/SettingsRoute.tsx to match
# so the About page's "Support Adaka" link goes live at the same time.
#
# github: [adakahq]
# open_collective: adaka
# ko_fi: adakahq
# custom: ["https://example.com/donate"]
